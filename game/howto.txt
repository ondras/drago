- mame 3 vrstvy:
  1. pozadi se predrenderuje z:
    - layer0
    - layer1 tam, kde neni top
	- animace layer0 i layer1 jsou actori v prvni vrstve; pouzije se clear_none
    
  2. hraci
  
  3. layer1 top

- actori si udrzuji px pozici vuci portu
- zmena viewportu => signal + dirty
